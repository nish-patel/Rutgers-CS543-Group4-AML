
 G.layout={
		layered:{
			shader:`
			//...
			`,
			input:{
				//interestingly, different layouts - 3D and 2D - may use different data representations, like one with nodes and the other with vertices, or a subset of nodes. (or it's OK if they merely apply different filters to underlying possible objects, and even the 2D layout may still respect height, and may even treat different layers as independent. lines are a special case - first they correspond to vertices, and then they may need to show as curves.
				//Since 
			}
			
		},
		flat:{
			
		},
		
	};